<!-- rendered from docs/research/RESEARCH-RADAR.json by scripts/render_research_radar.py; do not edit by hand -->
# Research Evidence Strength
| delta | strength | replication | why this strength | what would raise it |
|---|---|---|---|---|
| `RD-01` | `PRELIMINARY` | not replicated by LOGOS-1; single external source as reported | a single reported benchmark result with no decomposition or replication | independent replication + decomposition of the four factors |
| `RD-02` | `LOW_TO_MEDIUM` | not replicated by LOGOS-1; single external source as reported | conceptual argument with reported calibration gaps; not measured here | a validated TrajectoryConfidence metric with causal discrimination |
| `RD-03` | `LOW` | not replicated by LOGOS-1; single external source as reported | single reported study, no replication | replication on a LOGOS-controlled environment |
| `RD-04` | `LOW_TO_MEDIUM` | not replicated by LOGOS-1; single external source as reported | definitional separation is sound; empirical surface unreplicated | a measured surface with validated fidelity metrics |
| `RD-05` | `MEDIUM` | not replicated by LOGOS-1; single external source as reported | consistent with the RAD/MBG evidence class already reproduced deterministically here | independent replication with real agents |
| `RD-06` | `LOW_TO_MEDIUM` | not replicated by LOGOS-1; single external source as reported | single reported study; the invariant is a design principle, not a measured effect | embodied replication |
| `RD-07` | `MEDIUM_TO_HIGH` | not replicated by LOGOS-1; single external source as reported | methodological consensus in measurement science; converges with the repository's instrument-first rule | n/a — gate, not a finding |
| `RD-08` | `LOW_TO_MEDIUM` | not replicated by LOGOS-1; single external source as reported | architectural proposal with reported gains; unreplicated | ablation with validated metrics |
| `RD-09` | `LOW_TO_MEDIUM` | not replicated by LOGOS-1; single external source as reported | threat model is sound by construction; no empirical attack replicated | a reproduced poisoning attack against a real world model |
| `RD-10` | `MEDIUM` | not replicated by LOGOS-1; single external source as reported | reported attack class consistent with RAD-CE1 mechanics; the provenance remedy is tested here | real-agent ablation |
| `RD-11` | `LOW_TO_MEDIUM` | not replicated by LOGOS-1; single external source as reported | evidence chain (ground truth -> decodability -> intervention -> prediction change) is the right method; not run here | a causal intervention result on a real model |
| `RD-12` | `MEDIUM` | not replicated by LOGOS-1; single external source as reported | the causal chain matches the repository's own RAD-CE1 and consolidation evidence; the external scale is unreplicated | independent multi-agent replication |
| `RD-13` | `LOW` | not replicated by LOGOS-1; single external source as reported | single conceptual source | a measured persistence-dynamics result |
| `RD-14` | `MEDIUM` | not replicated by LOGOS-1; single external source as reported | mechanism is reproducible deterministically; the external empirical magnitude is unreplicated | replication on a learned memory |

No entry is `INDEPENDENTLY_REPLICATED` by LOGOS-1. Strength is assigned to the finding as it reaches this repository, not to the paper's own claims.
