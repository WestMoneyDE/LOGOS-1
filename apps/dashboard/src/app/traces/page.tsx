import { PhasePlaceholder } from "@/components/phase-placeholder";

export default function Page() {
  return <PhasePlaceholder title="Traces" phase={4} spec="§24 Trace Explorer" will={["Span-Baum / Wasserfall, Modell, Tokens, Quota, Artefakte, Claim-IDs", "Deep Link zur MLflow Trace UI", "Re-Score ohne neue Inferenz"]} />;
}
