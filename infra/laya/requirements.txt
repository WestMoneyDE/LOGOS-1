# logos-laya service — exact pins. torch is installed separately from the PyTorch CPU index
# (see Dockerfile) so the CUDA wheels are never pulled. Versions match the set the in-session
# measurements ran on (jev-ultrafast-laya, laya 0.3.6); a change here is a measured change.
laya==0.3.6
transformers==5.17.0
tokenizers==0.23.2
huggingface_hub==1.32.0
hf-xet==1.6.0
safetensors==0.8.0
numpy==2.5.3
fastapi==0.141.1
starlette==1.6.0
pydantic==2.13.5
pydantic_core==2.46.5
uvicorn==0.53.0
